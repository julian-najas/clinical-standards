# Example Python Service

This is a minimal consumer project used by the repository self-tests.

- PASS path excludes the intentional failing test.
- FAIL path runs the full test suite to confirm deterministic failure behavior.

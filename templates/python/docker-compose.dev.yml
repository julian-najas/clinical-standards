services:
  app:
    working_dir: /workspace
    command: >
      sh -lc "uvicorn ${UVICORN_APP:-app.main:app} --host 0.0.0.0 --port ${PORT:-8000} --reload"
    environment:
      PYTHONPATH: /workspace/src
    volumes:
      - ./:/workspace:cached
    ports:
      - "8000:8000"
